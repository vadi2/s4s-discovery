apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "s4s-cluster.fullname" . }}-test-connection"
  labels:
    {{- include "s4s-cluster.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "s4s-cluster.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
