apiVersion: v1
kind: Service
metadata:
  name: {{ include "s4s-cluster.fullname" . }}
  labels:
    {{- include "s4s-cluster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "s4s-cluster.selectorLabels" . | nindent 4 }}
